from setuptools import setup, find_packages

setup(name='jkl_serialization',
      version='0.1',
      description='(De)Serialization of JKL objects',
      author='Daan Knoope',
      author_email='daan@knoope.dev',
      license='MIT',
      packages=find_packages(), install_requires=[],
      python_requires='>3.6'
)